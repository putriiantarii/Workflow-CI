name: mlflow-env
channels:
  - conda-forge
dependencies:
  - python=3.9
  - pip
  - pip:
      - mlflow
      - pandas
      - numpy
      - scikit-learn
      - matplotlib
      - seaborn
      - dagshub
